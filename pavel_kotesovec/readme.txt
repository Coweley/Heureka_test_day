I created aplication with some features :
1) Ping test if server is accesible
2) Get Shop from database with spicific ID
    if is not exist you will get message "Shop not found"
    if id is not integer zou will get message "value is not a valid integer"

In future is possile to add functions for Put shops in database, Remove shops from database.....

For this I use FASTAPI, because it have realy good documentaion.